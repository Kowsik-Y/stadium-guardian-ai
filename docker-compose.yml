services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stadium-guardian-ai
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
    restart: always
